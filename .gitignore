# dependencies
node_modules/
.pnpm-store/

# build outputs
dist/
build/

# env — Convex CLI writes .env.local with the dev deployment URL
.env
.env.local
.env.*.local

# Convex local deployment runtime state (holds admin keys — never commit)
.convex/

# local reference material (rulebook markdown / PDFs) — used during dev, never committed
.reference/

# editor
.vscode/
.idea/
*.swp
.DS_Store

# local agent settings (user-specific)
.claude/settings.local.json

# test coverage
coverage/

# logs
*.log

# OS
Thumbs.db
