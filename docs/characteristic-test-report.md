# Characteristic-test family — slice report (2026-08-25)

## Definition of done — met

The characteristic-test family accounts for **29 exact instructions across 24
artifacts** (monsters-only at pin `520553438a4e8d199bfaaf676b8aa9bd273f4d61`).
All 29 execute canonically through the certified power-roll core; every
unsupported variation (25 family outliers and all other table directives)
remains verbatim; the full 1,688-program corpus re-executes with **zero
conservation or invariant violations**. Corpus accounting is now
**41 automated (12 exact + 29 test) · 1,647 table directives, of which 12 are
permanently-manual by user ruling and 1,635 mechanism-pending**.

## Rulings implemented (canon-rulings.md, approved 2026-08-25)

- **R-0006** — tests resolve through the unchanged power-roll core; natural
  19–20 is recorded as a test **critical success** on the roll receipt.
- **R-0007** — each creature target rolls independently with their OWN named
  characteristic; object targets never roll and auto-obtain tier 1
  [rule.combat/target], surfaced as an informational entry plus the exact
  tier-1 bullet as a directive.
- **R-0008** — no DC, no roll modifier; the statblock's tier table displaces
  generic difficulty outcome-mapping in this slice.
- **R-0009** — skills are rejected at the schema level (no skill input
  exists); sourced edges/banes and attributed numeric modifiers remain valid.
- **R-0010** — Assist is unavailable on creature/DTO reactive tests.
- **R-0011** — all **87** tier bullets attach losslessly: **21 automatic**
  (whole-payload-exact per the strict tier grammar) / **66 verbatim**
  tier-level directives. A test with any missing/extra band compiles as a
  plain table directive — never a partial program.

## Hardening the rulings forced first (both were latent certified defects)

1. **Cluster-ownership leak** (`groupPowerRollClusters`, one home): tier
   bullets no longer attach across an intervening ability header, Effect
   line, flavor, or residue prose. The old 606-cluster certification
   contained 4 counterfeit assemblies and 1 silent precondition loss, and
   suppressed 16 valid abilities.
2. **Strict whole-payload tier tails**: the tail check compared condition
   COUNT, not names — swallowing forced movement, resource grants,
   un-anchored potency gates, mid-payload endings, and duration tails. 34
   lossy clusters fell back to verbatim. Honest power-roll baseline:
   **583 clusters / 504 artifacts / 1,749 clean runs**.

## Verification channels

- Independent golden (`TEST_EFFECT_CANON_EXPECTATIONS`): 29 entries / 87
  bullets generated by a raw physical-line scanner with a separate minimal
  payload reader, cross-verified against production compilation with **zero
  field mismatches**, then frozen. The exhaustive suite executes every test
  at all three tiers against the GOLDEN data (deterministic dice per band)
  plus object-target auto-tier-1 runs, and freezes the
  5 damage / 7 condition / 29 test / 1,647 table split.
- Adversarial negatives: verbatim outliers (roar lead-in, fire-immunity
  rider, highest-characteristic form) stay table; rider-bearing bullets stay
  verbatim.
- Hosts: Convex recompiles ordinal-addressed programs from SHA-verified
  stored canon and auto-rolls per target with the seeded, replayable RNG;
  CLI supports multi-target + `obj:<label>` tokens; the Table adds object
  targets and the knockout choice. Backend end-to-end test drives the real
  devil-adjudicator record (drift-guarded verbatim fixture) through the full
  stack.
- CI: the corpus certification stamp gate (`pnpm corpus:certify` /
  `corpus:verify`) blocks any rule-semantics change that skipped a green
  corpus-enabled run.

## Known scope cuts (tracked, not hidden)

- Grabbed/slowed/taunted derived movement/roll rules remain named mechanism
  gaps (pre-existing).
- The Sanguine-Mist-class bullets (explicit end-of-encounter durations,
  mixed per-condition endings, movement riders) stay verbatim until their
  endings/mechanisms are typed and certified.
- Post-tier trailing prose remains attributed grammar residue (visible in
  the sweep, never claimed).
- Per-target knockout choice and turn-anchored end-of-next-turn expiry
  remain filed follow-ups from the Effect-pipeline review.
